/**
 * Dashboard page — main GIS workspace.
 * Composes the full-screen map with all overlay panels.
 * This is the primary (and only) page in the SPA.
 */
export { default as DashboardPage } from '../App';
